// Copy this file to `firebaseConfig.ts` and fill with your Firebase web config.
// Keep this file out of source control for secrets. Example:
// export const firebaseConfig = {
//   apiKey: 'YOUR_KEY',
//   authDomain: 'your-app.firebaseapp.com',
//   projectId: 'your-app',
//   storageBucket: 'your-app.appspot.com',
//   messagingSenderId: '...',
//   appId: '1:...:web:...',
// };

// Optional: set `autoEnable` to true and provide `userId` for automatic sync
// at app startup. Otherwise call `enableSyncForUser(database, userId)` after
// successful sign-in.
export const firebaseConfig: null = null;
